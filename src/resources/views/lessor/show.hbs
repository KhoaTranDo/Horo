<h1>Your rental room</h1>

<div class="col-sm-6 col-lg-4">
    <div class="card card-course-item">
        <img class="card-img-top" src="/upload/{{roles.image}}" alt="{{roles.location}}">
        <div class="card-body">
            <h3 class="card-title">{{roles.room}}</h3>
            <h3 class="card-title">Owner : {{roles.slug}} </h3>
            <h3 class="card-title">Price : {{roles.price}} VNĐ</h3>
            <h3 class="card-title">Address : {{roles.address}}</h3>
        </div>
    </div>
</div>

<div>
    <form method="POST" action="/lessor/store?_method=PUT">
        <div class="card-body">
            <h1>Your Feedback</h1>
            <input type="text" name="feedback">
        </div>
        <button type="submit" class="btn btn-primary" style="width: 30%;">Feedback</button>
    </form>
</div>
<a href="./feedback">Link Feedback</a>